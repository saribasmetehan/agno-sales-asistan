version: '3.8'

services:
  app:
    build: .
    container_name: sales-assistant-app
    ports:
      - "8501:8501"
    env_file:
      - .env
    restart: unless-stopped
    volumes:
      - ./:/app
